<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- load .properties file -->
    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer">
        <property targetName="locations" value="classpath:badromance.properties"/>
    </bean>

    <!-- spring servlet will handle all requests, including static -->
    <mvc:default-servlet-handler/>

    <!-- check out those awesome annotations @Component, @Controller, @Service && @Repository-->
    <mvc:annotation-driven/>

    <!-- spring will check for components from this package -->
    <context:component-scan base-package="org.academiadecodigo.olympiands.badromance"/>

    <!-- default targetName is transaction manager -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property targetName="entityManagerFactory" ref="emFactory"/>
    </bean>

    <import resource="./persistence/h2.xml"/>
    <import resource="./persistence/mysql.xml"/>

</beans>
